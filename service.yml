apiVersion: v1
kind: Service
metadata:
  name: Book-My-Show-service
  labels:
    app: Book-My-Show
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000  # Replace with the port your app runs on
  selector:
    app: bms
